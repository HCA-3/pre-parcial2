crear entorno virtual:

python --version
python -m venv venv
source venv/Scripts/activate
venv\Scripts\actívate

git status
git add .
git commit -m "texto"
git push -u origin (master/main)
deactivate
